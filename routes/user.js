var express = require('express');
var LocalStrategy = require('passport-local').Strategy;
var utils = require('../config/utils');
var router = express.Router();
var passport = require('passport');
var User = require('../models/User');
var bCrypt = require('bcrypt-nodejs');

module.exports = function (passport) {
/* GET users listing. */




// note there is for the learner of nodejs if you use get mehtod to get request entity you should use req.query to get the data 
// if you want to use the  post you should use the req.body and if you want to get the info from url you should use req.params



return router;
}

